version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: esilogis-back-test
    ports:
      - "3001:3001"
    env_file: 
      - .env
    restart: unless-stopped
